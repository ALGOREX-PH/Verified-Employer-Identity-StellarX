# Rust / Soroban
/target
**/*.rs.bk
test_snapshots/


# Node / Next.js (the web/ app keeps its own .gitignore too)
node_modules
.next
*.log

# Local env files (never commit secrets)
.env
.env.local
.env*.local

# Agent scratch (worktrees, SDD ledgers)
.claude/worktrees/
.superpowers/

# OS / editor
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json
